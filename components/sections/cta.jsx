import { Button } from '@/components/ui/button'

const CTASection = ({ data = {} }) => {
  console.log(data)
  return (
    <section>
      <div>CTA</div>
    </section>
  )
}

export default CTASection
