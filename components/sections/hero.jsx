import { Button } from '@/components/ui/button'

const HeroSection = ({ data = {} }) => {
  console.log(data)
  return (
    <section>
      <div>CTA</div>
    </section>
  )
}

export default HeroSection
